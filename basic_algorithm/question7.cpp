/*
  Write a C++ program to exchange the first and 
  last characters in a given string and return 
  the result string
*/

#include <iostream>
#include <string>

int main() {
    std::string name{"Yes"};
    std::string test{};
    test.replace(1,4.name);
}